CI Upgrade Summary - Step 6

This document summarizes the successful upgrade of the CI infrastructure to the Post-Human standard.

Key achievements:
- All instances of `actions/upload-artifact@v3` were upgraded to `v4`.
- All instances of `actions/checkout@v3` were upgraded to `v4`.
- All instances of `actions/setup-python@v4` were upgraded to `v5`.
- All instances of `dorny/paths-filter@v3` were upgraded to `v4`.
- All instances of `nick-invision/retry@v3` were upgraded to `v4`.
- The `required-ci.yml` workflow is now fully compatible with the post-deprecation GitHub runner environment.

The CI pipeline has been successfully resurrected and aligned with post-human engineering standards.
