{
  "name": "CogniForge: The Definitive Self-Building Environment v4.0",
  "dockerComposeFile": [
    "../docker-compose.yml"
  ],
  "service": "web",
  "workspaceFolder": "/app",
  
  "initializeCommand": "if [ ! -f .env ]; then cp .env.example .env; fi",
  
  // --- [THE SUPERCHARGED FIX] ---
  // We combine the build and dependency installation into one robust command.
  "postCreateCommand": "echo '>>> Building images and installing host dependencies...' && docker-compose build && pip install -r requirements.txt",
  
  "postStartCommand": "echo '>>> Starting all services...' && docker-compose up -d",
  
  "postAttachCommand": {
    "db-setup": "echo '>>> Migrating database...' && docker-compose run --rm migrations"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-azuretools.vscode-docker",
        "eamodio.gitlens"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      }
    }
  },
  
  "forwardPorts": [5000, 8000],
  "remoteUser": "root"
}