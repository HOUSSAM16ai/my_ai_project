# 🏗️ بنية المشروع - CogniForge

## 📊 نظرة عامة

المشروع منظم بشكل منطقي وبسيط لسهولة الفهم والصيانة.

---

## 🗂️ البنية الكاملة

```
app/
│
├── 📄 __init__.py                 # ملف التهيئة الرئيسي
├── 📄 main.py                     # نقطة الدخول (Entry Point)
├── 📄 kernel.py                   # قلب النظام (System Core)
├── 📄 models.py                   # نماذج قاعدة البيانات
│
├── 📁 api/                        # واجهات برمجة التطبيقات
│   ├── 📄 dependencies.py         # الاعتماديات المشتركة
│   ├── 📄 exceptions.py           # معالجة الأخطاء
│   └── 📁 routers/                # المسارات (Endpoints)
│       ├── 📄 admin.py            # مسارات المسؤول
│       ├── 📄 crud.py             # عمليات CRUD
│       ├── 📄 data_mesh.py        # شبكة البيانات
│       ├── 📄 observability.py    # المراقبة
│       ├── 📄 security.py         # الأمان
│       └── 📁 system/             # مسارات النظام
│           └── 📄 __init__.py     # صحة النظام (Health)
│
├── 📁 config/                     # الإعدادات
│   ├── 📄 settings.py             # إعدادات التطبيق
│   ├── 📄 ai_models.py            # إعدادات الذكاء الاصطناعي
│   └── 📄 superhuman_config.py    # إعدادات متقدمة
│
├── 📁 core/                       # الوظائف الأساسية
│   ├── 📄 database.py             # إدارة قاعدة البيانات
│   ├── 📄 security.py             # الأمان والمصادقة
│   ├── 📄 di.py                   # Dependency Injection
│   ├── 📄 ai_gateway.py           # بوابة الذكاء الاصطناعي
│   ├── 📄 error_handling.py       # معالجة الأخطاء
│   ├── 📄 static_handler.py       # معالج الملفات الثابتة
│   │
│   ├── 📁 engine/                 # محرك قاعدة البيانات
│   │   ├── 📄 factory.py          # مصنع المحرك
│   │   ├── 📄 types.py            # الأنواع
│   │   └── 📄 pooler.py           # إدارة الاتصالات
│   │
│   ├── 📁 gateway/                # بوابة الذكاء الاصطناعي
│   │   ├── 📄 service.py          # الخدمة الرئيسية
│   │   ├── 📄 routing.py          # التوجيه
│   │   ├── 📄 cache.py            # التخزين المؤقت
│   │   ├── 📁 providers/          # مزودو الخدمة
│   │   │   ├── 📄 openai.py       # OpenAI
│   │   │   ├── 📄 anthropic.py    # Anthropic
│   │   │   └── 📄 base.py         # القاعدة
│   │   └── 📁 protocols/          # البروتوكولات
│   │       ├── 📄 rest.py         # REST API
│   │       ├── 📄 grpc.py         # gRPC
│   │       └── 📄 graphql.py      # GraphQL
│   │
│   └── 📁 resilience/             # المرونة والاستقرار
│       └── 📄 circuit_breaker.py  # قاطع الدائرة
│
├── 📁 middleware/                 # الطبقات الوسيطة
│   ├── 📄 fastapi_error_handlers.py
│   ├── 📄 remove_blocking_headers.py
│   │
│   ├── 📁 security/               # الأمان
│   │   ├── 📄 security_headers.py
│   │   ├── 📄 rate_limit_middleware.py
│   │   ├── 📄 waf_middleware.py
│   │   └── 📄 zero_trust_middleware.py
│   │
│   └── 📁 core/                   # الأساسيات
│       ├── 📄 base_middleware.py
│       ├── 📄 pipeline.py
│       └── 📄 context.py
│
├── 📁 schemas/                    # مخططات البيانات (Pydantic)
│   ├── 📄 management.py           # إدارة
│   └── 📄 observability.py        # مراقبة
│
├── 📁 security/                   # خدمات الأمان
│   ├── 📄 secure_auth.py          # المصادقة الآمنة
│   ├── 📄 zero_trust.py           # الثقة المعدومة
│   ├── 📄 chrono_shield.py        # الحماية الزمنية
│   ├── 📄 encryption.py           # التشفير
│   ├── 📄 waf.py                  # جدار الحماية
│   ├── 📄 rate_limiter.py         # تحديد المعدل
│   └── 📄 threat_detector.py      # كشف التهديدات
│
└── 📁 services/                   # الخدمات الأساسية
    ├── 📄 user_service.py         # إدارة المستخدمين
    ├── 📄 system_service.py       # صحة النظام
    ├── 📄 database_service.py     # قاعدة البيانات
    └── 📄 history_service.py      # السجل
```

---

## 📚 شرح المجلدات

### 1. 📁 api/ - واجهات برمجة التطبيقات
**الدور**: يحتوي على جميع نقاط النهاية (Endpoints) التي يمكن للمستخدمين الوصول إليها.

**الملفات الرئيسية**:
- `routers/` - المسارات المنظمة حسب الوظيفة
- `dependencies.py` - الاعتماديات المشتركة
- `exceptions.py` - معالجة الأخطاء

**مثال**:
```python
# api/routers/system/__init__.py
@router.get("/health")
async def health_check():
    return {"status": "healthy"}
```

---

### 2. 📁 config/ - الإعدادات
**الدور**: يحتوي على جميع إعدادات التطبيق.

**الملفات الرئيسية**:
- `settings.py` - الإعدادات العامة
- `ai_models.py` - إعدادات نماذج الذكاء الاصطناعي

**مثال**:
```python
# config/settings.py
class AppSettings(BaseSettings):
    PROJECT_NAME: str = "CogniForge"
    DATABASE_URL: str
```

---

### 3. 📁 core/ - الوظائف الأساسية
**الدور**: يحتوي على الوظائف الأساسية التي يستخدمها كل المشروع.

**المجلدات الفرعية**:
- `engine/` - محرك قاعدة البيانات
- `gateway/` - بوابة الذكاء الاصطناعي
- `resilience/` - المرونة والاستقرار

**مثال**:
```python
# core/database.py
async def get_db():
    async with async_session_factory() as session:
        yield session
```

---

### 4. 📁 middleware/ - الطبقات الوسيطة
**الدور**: يحتوي على الطبقات التي تعالج الطلبات قبل وصولها للـ API.

**الأنواع**:
- `security/` - الأمان
- `core/` - الأساسيات

**مثال**:
```python
# middleware/security/rate_limit_middleware.py
class RateLimitMiddleware:
    async def __call__(self, request, call_next):
        # تحديد عدد الطلبات
        return await call_next(request)
```

---

### 5. 📁 security/ - خدمات الأمان
**الدور**: يحتوي على جميع خدمات الأمان والحماية.

**الملفات الرئيسية**:
- `secure_auth.py` - المصادقة
- `encryption.py` - التشفير
- `waf.py` - جدار الحماية

**مثال**:
```python
# security/secure_auth.py
def verify_password(plain, hashed):
    return pwd_context.verify(plain, hashed)
```

---

### 6. 📁 services/ - الخدمات الأساسية
**الدور**: يحتوي على منطق العمل (Business Logic).

**الخدمات**:
- `user_service.py` - إدارة المستخدمين
- `system_service.py` - صحة النظام
- `database_service.py` - قاعدة البيانات
- `history_service.py` - السجل

**مثال**:
```python
# services/user_service.py
class UserService:
    async def get_all_users(self):
        return await self.db.execute(select(User))
```

---

## 🎯 مبادئ التنظيم

### 1. الفصل حسب الوظيفة
كل مجلد له وظيفة واضحة ومحددة.

### 2. البساطة
البنية بسيطة وسهلة الفهم.

### 3. القابلية للتوسع
يمكن إضافة ميزات جديدة بسهولة.

### 4. الوضوح
كل ملف موثق ومفهوم.

---

## ✅ الخلاصة

البنية الحالية:
- ✅ منطقية ومنظمة
- ✅ سهلة الفهم
- ✅ قابلة للتوسع
- ✅ موثقة بالكامل

---

**آخر تحديث**: 2024-12-20  
**الحالة**: ✅ مكتمل  
**الجودة**: ممتازة
