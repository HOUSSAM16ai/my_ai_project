# 🧠 دليل إدخال المعرفة المتقدم (Advanced Knowledge Ingestion)

يوضح هذا المستند كيفية تحويل المصادر التعليمية (كتب، ملفات، صور) إلى "ذاكرة حية" داخل نظام CogniForge، وكيفية التعامل مع الملفات الضخمة والوسائط المتعددة.

## 1. دورة حياة المعرفة (The Knowledge Lifecycle)

النظام مصمم ليكون "مستودعاً للمعرفة" وليس "أرشيفاً للملفات".

1.  **الإدخال (Ingestion):** يتم رفع الملف (PDF, DOCX, XLSX, Image) أو إرساله للنظام.
2.  **التحويل (Transformation):**
    *   يتم استخراج النصوص باستخدام أدوات متخصصة (`pypdf`, `python-docx`).
    *   يتم تطبيق OCR على الصور لاستخراج النصوص المدمجة.
    *   يتم تقسيم المحتوى إلى "وحدات معرفية" (Chunks) مترابطة دلالياً.
3.  **التخزين (Storage):** تُحفظ الوحدات في `Memory Agent` كمتجهات (Embeddings) وقواعد بيانات دلالية.
4.  **الحذف (Deletion):** **يمكن حذف الملف الأصلي بأمان.** المعرفة أصبحت الآن جزءاً من "عقل" النظام ولا تعتمد على وجود الملف الفيزيائي.

---

## 2. طرق إيصال البيانات (Ingestion Methods)

يوفر النظام عدة قنوات لتغذية وكيل الذاكرة:

### أ. الرفع التلقائي (GitHub Knowledge Base) - *الطريقة الموصى بها*
*   **كيف:** ارفع الملفات إلى مجلد `knowledge_base/` في المستودع.
*   **الآلية:** يعمل `GitHub Action` تلقائياً، يعالج الملفات، يرسلها للوكيل، ويصدر تقريراً.
*   **الميزة:** أتمتة كاملة، دعم للملفات الضخمة (Streaming).

### ب. الإدخال اليدوي (CLI Tool)
*   **كيف:** استخدام الأداة سطر الأوامر `scripts/ingest_knowledge.py`.
*   **الاستخدام:**
    ```bash
    python scripts/ingest_knowledge.py my_book.pdf --url http://localhost:8001/memories
    ```
*   **الميزة:** تحكم دقيق، تجربة محلية سريعة.

### ج. عبر الوكيل الذكي (AI Assistant)
*   يمكنك طلب ذلك مباشرة من المساعد الذكي (Jules): *"من فضلك حول هذا الملف المرفق إلى ذاكرة."*
*   سيقوم الوكيل باستخدام أدواته الداخلية (`read_file`, `ingest_knowledge`) لتنفيذ المهمة نيابة عنك.

### د. واجهة برمجة التطبيقات (Direct API)
*   للمطورين، يمكن إرسال وحدات JSON مباشرة:
    ```json
    POST /memories
    {
      "content": "نص الفقرة التعليمية...",
      "tags": ["فيزياء", "درس_1"]
    }
    ```

---

## 3. التعامل مع الصور والرسومات (Images & Charts)

النظام الحالي يدعم الوسائط المرئية بالمستويات التالية:

### ✅ المستوى الحالي: التعرف النصي (OCR)
*   **التقنية:** `Tesseract OCR`.
*   **ماذا يفعل:** يقرأ أي نص مكتوب *داخل* الصورة أو المخطط (مثل عناوين المحاور، الأرقام، المسميات التوضيحية).
*   **الحالة:** مفعل وجاهز.

### 🚧 المستوى المستقبلي: الفهم البصري (Vision Understanding)
*   **التقنية:** `Vision LLMs` (مثل GPT-4o, LLaVA).
*   **ماذا سيفعل:** يصف *محتوى* الصورة ("هذا رسم بياني يوضح ارتفاع المبيعات بنسبة 20%").
*   **الحالة:** مخطط له في التحديثات القادمة.

**نصيحة لتحسين النتائج حالياً:** عند استخدام صور أو رسومات بيانية، حاول أن تكون النصوص التوضيحية بداخلها واضحة، أو أرفق نصاً وصفياً بجانب الصورة في الملف الأصلي.

---

## 4. الكتب الضخمة (Massive Books)

*   **المشكلة:** ملفات PDF بحجم 1GB+ تستهلك الذاكرة.
*   **الحل:** تقنية **Streaming**.
    *   النظام لا يقرأ الكتاب كاملاً.
    *   يقرأ صفحة -> يعالجها -> يرسلها -> يفرغ الذاكرة -> ينتقل للصفحة التالية.
*   **النتيجة:** يمكنك معالجة مكتبة كاملة دون قلق بشأن ذاكرة الخادم (RAM).
