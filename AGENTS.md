# تعليمات للوكلاء (Agents) - CogniForge
# =============================================================================

هذا الملف يحتوي على تعليمات صارمة يجب على جميع الوكلاء (Agents) والمطورين الالتزام بها عند التعامل مع قاعدة الكود.

## 🛡️ حماية الأصول الحيوية (Critical Assets Protection)

لقد تم تفعيل نظام حماية صارم لمنع حذف السكريبتات الأساسية لبيئة التطوير والتشغيل.

### الملفات المحمية
يمنع منعًا باتًا حذف أو تعطيل الملفات التالية:
1. `scripts/setup_dev.sh`
2. `scripts/setup_pre_commit.sh`
3. `scripts/start.sh`

### آليات الحماية
تم تطبيق الحماية عبر طبقتين:

1. **Local Pre-commit Hook**:
   - يوجد سكريبت فحص تلقائي `scripts/check_critical_assets.py`.
   - يتم تشغيله تلقائيًا قبل أي عملية `commit`.
   - إذا كانت الملفات مفقودة، سيتم رفض الـ commit.

2. **CI/CD Pipeline Validation**:
   - يوجد اختبار `tests/infrastructure/test_critical_assets.py`.
   - يعمل هذا الاختبار ضمن دورة `make test` و GitHub Actions.
   - إذا فشل الاختبار، سيفشل الـ Build بالكامل.

### تعليمات للوكلاء
- **تحقق دائمًا**: قبل اقتراح أي تغييرات تتضمن حذف ملفات، تأكد أنها ليست ضمن القائمة المحمية.
- **لا تعطل الحماية**: لا تقم بتعديل `scripts/check_critical_assets.py` أو `.pre-commit-config.yaml` لإزالة هذه الحماية إلا بأمر صريح ومبرر.
- **التوثيق**: أي أصول حيوية جديدة يجب إضافتها إلى قائمة الحماية في السكريبت وملف الاختبار.

## ✅ معايير الجودة
- يجب تشغيل `make quality` قبل تسليم أي كود.
- يجب التأكد من أن التغطية (Code Coverage) لا تقل عن المعايير المحددة في `PROJECT_METRICS.md`.
