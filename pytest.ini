[pytest]
asyncio_mode = auto
python_files = test_*.py
addopts = --import-mode=importlib
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    security: اختبارات تركز على سلوكيات الأمان والتحقق الصارم
