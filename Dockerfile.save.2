FROM python:3.12-slimFROM python:3.12-slimFROM python:3.12-slimFROM python:3.12-slimFROM python:3.12-slim

nano Dockerfilenano Dockerfile# 1. استخدم صورة Python أساسية
FROM python:3.12-slim

# 2. قم بتعيين مجلد العمل داخل الحاوية
WORKDIR /app

# 3. انسخ ملف متطلبات المكتبات (إذا كان لديك واحد)
# إذا لم يكن لديك ملف requirements.txt، يمكنك حذف السطرين التاليين أو وضع # قبلهما
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 4. انسخ كل ملفات مشروعك الحالي إلى مجلد العمل داخل الحاوية
COPY . .

# 5. الأمر الافتراضي الذي سيتم تشغيله عند بدء الحاوية
# استبدل app.py باسم ملف Python الرئيسي لتطبيقك
CMD ["python", "app.py"]

