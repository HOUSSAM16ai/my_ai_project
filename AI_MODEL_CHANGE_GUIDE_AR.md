# 🤖 دليل تغيير نموذج الذكاء الاصطناعي

## السؤال: كيف أغير نموذج AI من Claude إلى ChatGPT؟

### 📍 الموقع الرئيسي: ملف `.env`

لتغيير نموذج الذكاء الاصطناعي، افتح ملف **`.env`** في جذر المشروع وابحث عن المتغير:

```bash
DEFAULT_AI_MODEL="anthropic/claude-3.7-sonnet:thinking"
```

### ✅ الأسماء الكاملة لنماذج ChatGPT (OpenRouter)

قم بتغيير القيمة إلى أحد النماذج التالية:

| النموذج | الاسم الكامل للتكوين | الوصف |
|---------|---------------------|-------|
| GPT-4o (الأحدث والأفضل) | `openai/gpt-4o` | أقوى نموذج متعدد الوسائط |
| GPT-4o Mini (سريع وموفر) | `openai/gpt-4o-mini` | سريع ومنخفض التكلفة |
| GPT-4 Turbo | `openai/gpt-4-turbo` | نسخة محسنة من GPT-4 |
| GPT-4 | `openai/gpt-4` | النموذج الكلاسيكي |
| GPT-3.5 Turbo | `openai/gpt-3.5-turbo` | سريع جداً ومنخفض التكلفة |

### 🔧 مثال على التغيير

**قبل (Claude):**
```bash
DEFAULT_AI_MODEL="anthropic/claude-3.7-sonnet:thinking"
```

**بعد (ChatGPT):**
```bash
DEFAULT_AI_MODEL="openai/gpt-4o"
```

### 📁 ملفات التكوين ذات الصلة

1. **`.env`** - ملف البيئة الرئيسي (هذا هو المكان الذي يجب تعديله)
2. **`.env.example`** - مثال للتكوين (للمرجع فقط)
3. **`app/config/settings.py`** - إعدادات التطبيق (القيمة الافتراضية)
4. **`app/settings.py`** - إعدادات إضافية

### 🎯 المتغيرات الأخرى المتعلقة بالنماذج

يمكنك أيضاً تخصيص هذه المتغيرات في ملف `.env`:

```bash
# النموذج الافتراضي الرئيسي
DEFAULT_AI_MODEL="openai/gpt-4o"

# نموذج منخفض التكلفة للمهام البسيطة
LOW_COST_MODEL="openai/gpt-4o-mini"

# نماذج التوجيه الذكي (اختياري)
NANO_MODEL="openai/gpt-4o-mini"      # استعلامات سريعة وبسيطة
FAST_MODEL="openai/gpt-4o-mini"      # ردود سريعة
SMART_MODEL="openai/gpt-4o"          # ردود ذكية
GENIUS_MODEL="openai/gpt-4"          # استدلال معقد
```

### ⚠️ ملاحظات مهمة

1. **مفتاح OpenRouter**: تأكد من وجود `OPENROUTER_API_KEY` صالح في ملف `.env`
2. **إعادة التشغيل**: بعد تغيير النموذج، أعد تشغيل التطبيق
3. **التكلفة**: نماذج OpenAI قد تختلف في التكلفة - GPT-4o-mini أرخص من GPT-4o

### 🚀 خطوات التطبيق

1. افتح ملف `.env` في المحرر
2. ابحث عن `DEFAULT_AI_MODEL`
3. غيّر القيمة إلى `openai/gpt-4o` (أو أي نموذج آخر)
4. احفظ الملف
5. أعد تشغيل التطبيق:
   ```bash
   # إذا كنت تستخدم Docker
   docker-compose restart

   # أو إذا كنت تشغل Flask مباشرة
   flask run
   ```

---

**ملاحظة**: المشروع يستخدم **OpenRouter** كبوابة موحدة للوصول إلى جميع نماذج AI (OpenAI، Anthropic، وغيرها)، لذلك الصيغة هي دائماً: `provider/model-name`
