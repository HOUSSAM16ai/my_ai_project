apiVersion: apps/v1
kind: Deployment
metadata:
  name: cogniforge
spec:
  replicas: 5
  template:
    spec:
      containers:
        - name: cogniforge
          resources:
            requests:
              cpu: 1000m
              memory: 1Gi
            limits:
              cpu: 4000m
              memory: 4Gi
