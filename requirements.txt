# requirements.txt - The Strict Compatibility & Optimized Resource Protocol (vFinal)

# --- [RESOURCE OPTIMIZATION & COMPATIBILITY PROTOCOL] ---
# 1. We tell pip to find CPU-specific torch packages first.
--extra-index-url https://download.pytorch.org/whl/cpu

# 2. We pin specific, known-to-be-compatible versions to prevent dependency conflicts.
#    This is the key to solving the 'NoneType' error in SentenceTransformer.
torch==2.3.1
transformers==4.41.2
sentence-transformers>=2.6.1 # This will now use the exact torch/transformers versions above.
numpy>=1.26 # Explicitly required by the AI stack.
openai>=1.40.0
# --- نهاية البروتوكوك- ---


# --- Core Web Framework & Extensions ---
Flask
Flask-WTF
Flask-Login
Werkzeug
email-validator
gunicorn

# --- Database Layer ---
flask-sqlalchemy
flask-migrate
psycopg2-binary

# --- New Supercharged CLI ---
typer>=0.12.3
rich>=13.7.0

# --- Asynchronous Services (for ai_service) ---
fastapi
uvicorn

# --- API Enhancement & Validation ---
marshmallow>=3.20.0
flask-cors>=4.0.0
flasgger>=0.9.7.1
PyJWT>=2.7.0
cryptography>=41.0.0

# --- Development & Testing ---
python-dotenv
pytest
pytest-flask
requests