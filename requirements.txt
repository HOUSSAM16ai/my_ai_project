# requirements.txt (The Resource-Optimized Final Form)

# --- [RESOURCE OPTIMIZATION PROTOCOL] ---
# 1. نخبر pip بالبحث عن حزم torch المخصصة لوحدات المعالجة المركزية (CPU) من مستودع PyTorch الرسمي.
--extra-index-url https://download.pytorch.org/whl/cpu

# 2. نقوم بتثبيت torch CPU-only بشكل صريح. هذا يمنع sentence-transformers من سحب النسخة العملاقة
#    الخاصة بوحدات معالجة الرسومات (GPU).
torch
# --- نهاية البروتوكول ---


# --- Flask Core & Extensions ---
Flask
Flask-WTF
Flask-Login
Werkzeug
email-validator

# --- Database ORM & Migrations ---
flask-sqlalchemy
flask-migrate
psycopg2-binary

# --- WSGI Server for Production ---
gunicorn

# --- Utilities ---
python-dotenv

# --- AI & Vector DB Libraries ---
openai
chromadb-client
sentence-transformers # هذا سيكتشف أن torch مثبت بالفعل وسيستخدمه

# --- Testing & HTTP ---
pytest
pytest-flask
requests