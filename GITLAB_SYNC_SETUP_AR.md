# 🔄 دليل إعداد المزامنة مع GitLab

## 📋 نظرة عامة

تم تحسين GitHub Actions لتوفير الدقائق المخصصة من خلال:
1. ✅ الاحتفاظ فقط بفحص جودة الكود والاختبارات الأساسية
2. 🔄 تفعيل المزامنة التلقائية مع GitLab
3. ⏸️ تعطيل Workflows الثقيلة (تشتغل يدوياً فقط)

## 🎯 الملفات النشطة

### ملفات نشطة تلقائياً:
- ✅ `.github/workflows/ci.yml` - فحص الجودة والاختبارات الأساسية
- ✅ `.github/workflows/universal_sync.yml` - المزامنة مع GitLab

### ملفات معطلة (يدوية فقط):
- ⏸️ `.github/workflows/comprehensive_testing.yml` - اختبارات شاملة (يدوية)
- ⏸️ `.github/workflows/omega_pipeline.yml` - Omega pipeline (يدوية)

## 🔐 إعداد Secrets للمزامنة مع GitLab

### الخطوة 1: إنشاء Personal Access Token في GitLab

1. اذهب إلى GitLab: https://gitlab.com/-/profile/personal_access_tokens
2. اضغط "Add new token"
3. اختر الصلاحيات التالية:
   - ✅ `api` - Full API access
   - ✅ `write_repository` - Write to repository
4. احفظ الـ Token (سنحتاجه لاحقاً)

### الخطوة 2: الحصول على Project ID من GitLab

1. افتح مشروعك في GitLab
2. اذهب إلى Settings → General
3. ستجد "Project ID" في أعلى الصفحة (مثال: `12345678`)

### الخطوة 3: إنشاء Personal Access Token في GitHub

1. اذهب إلى GitHub: https://github.com/settings/tokens
2. اضغط "Generate new token (classic)"
3. اختر الصلاحيات التالية:
   - ✅ `repo` - Full control of repositories
4. احفظ الـ Token

### الخطوة 4: الحصول على Repository ID من GitHub

استخدم GitHub API:
```bash
# استبدل USERNAME و REPO_NAME بالقيم الصحيحة
curl https://api.github.com/repos/USERNAME/REPO_NAME | grep '"id"'
```

أو افتح المستودع وشاهد الـ URL، الرقم الموجود هو الـ ID.

### الخطوة 5: إضافة Secrets في GitHub

1. اذهب إلى مستودعك في GitHub
2. Settings → Secrets and variables → Actions
3. اضغط "New repository secret" لكل من:

| اسم الـ Secret | القيمة | الوصف |
|---------------|--------|-------|
| `SYNC_GITHUB_TOKEN` | Token من الخطوة 3 | للوصول إلى GitHub |
| `SYNC_GITHUB_ID` | Repository ID | رقم تعريف المستودع |
| `SYNC_GITLAB_TOKEN` | Token من الخطوة 1 | للوصول إلى GitLab |
| `SYNC_GITLAB_ID` | Project ID من الخطوة 2 | رقم تعريف المشروع |

## ✅ التحقق من المزامنة

### اختبار يدوي:

1. اذهب إلى مستودعك في GitHub
2. Actions → Universal Repository Synchronization Protocol
3. اضغط "Run workflow"
4. انتظر حتى ينتهي
5. تحقق من GitLab أن التغييرات تمت مزامنتها ✅

### المزامنة التلقائية:

بعد إضافة الـ Secrets، كل push إلى GitHub سيتم مزامنته تلقائياً مع GitLab:

```
GitHub (push) → Actions تشتغل → يزامن مع GitLab ✅
```

## 🔍 استكشاف الأخطاء

### المزامنة لا تعمل؟

1. **تحقق من الـ Secrets:**
   ```bash
   # في الـ workflow logs، تحقق من وجود رسائل مثل:
   # "SYNC_GITLAB_TOKEN not configured"
   ```

2. **تحقق من صلاحيات الـ Token:**
   - GitLab Token يجب أن يحتوي على `api` و `write_repository`
   - GitHub Token يجب أن يحتوي على `repo`

3. **تحقق من الـ Project ID و Repository ID:**
   - الأرقام يجب أن تكون صحيحة
   - استخدم الأوامر أعلاه للتحقق

4. **شاهد الـ Logs:**
   ```
   GitHub → Actions → Universal Repository Synchronization Protocol → أحدث تشغيل → Logs
   ```

### رسائل خطأ شائعة:

| الخطأ | الحل |
|------|-----|
| `Authentication failed` | تحقق من صحة الـ Token |
| `Project not found` | تحقق من الـ Project ID |
| `Permission denied` | أضف الصلاحيات المطلوبة للـ Token |

## 📊 توفير الدقائق

قبل التحسين:
- ❌ 4 workflows تعمل على كل push
- ❌ استهلاك كبير للدقائق المخصصة

بعد التحسين:
- ✅ 2 workflows فقط (ci + sync)
- ✅ Workflows الثقيلة يدوية فقط
- ✅ توفير كبير في الدقائق 🎉

## 🚀 الخطوات التالية

1. أضف الـ Secrets كما هو موضح أعلاه
2. اعمل Push لـ test commit
3. تأكد من المزامنة في GitLab
4. استمتع بتوفير الدقائق! 🎉

## 📚 ملفات مرتبطة

- `scripts/universal_repo_sync.py` - سكريبت المزامنة الرئيسي
- `.github/workflows/universal_sync.yml` - Workflow المزامنة
- `.github/workflows/ci.yml` - Workflow الجودة والاختبارات
- `.gitlab-ci.yml` - تكوين GitLab CI

## 🤝 الدعم

إذا واجهت أي مشاكل:
1. تحقق من الـ Logs في GitHub Actions
2. راجع هذا الدليل
3. تأكد من جميع الـ Secrets مضافة بشكل صحيح

---

✨ **تم بواسطة:** Houssam Benmerah  
📅 **التاريخ:** 2025-12-10
