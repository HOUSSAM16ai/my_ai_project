stages:
  - sync

hyper_sync:
  stage: sync
  image: python:3.12
  before_script:
    - pip install requests
  script:
    - python scripts/universal_repo_sync.py
  rules:
    - if: $CI_COMMIT_BRANCH
    - if: $CI_COMMIT_TAG
