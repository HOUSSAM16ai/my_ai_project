# 🔧 دليل استكشاف أخطاء المزامنة مع GitLab

## 🎯 المشكلة: المزامنة كانت تعمل ثم توقفت

إذا كانت المزامنة تعمل بشكل جيد ثم توقفت فجأة، هذه هي الأسباب الأكثر شيوعاً:

## ✅ التشخيص السريع

### الخطوة 1: تشغيل أداة التشخيص
```bash
python scripts/diagnose_gitlab_sync.py
```

هذه الأداة ستفحص:
- ✅ الكود والـ scripts
- ✅ الـ workflow configuration
- ✅ Git configuration
- ✅ Dependencies

## 🔍 الأسباب الشائعة والحلول

### 1. 🔑 GitLab Token منتهي الصلاحية (الأكثر شيوعاً)

**المشكلة:**
- GitLab Personal Access Tokens لها مدة صلاحية
- عند انتهاء الصلاحية، المزامنة تتوقف

**الحل:**
```bash
1. اذهب إلى GitLab: https://gitlab.com/-/profile/personal_access_tokens
2. تحقق من تاريخ انتهاء الـ Token الحالي
3. إذا كان منتهي أو قريب من الانتهاء:
   - أنشئ Token جديد
   - اختر الصلاحيات: api + write_repository
   - احفظ الـ Token
4. اذهب إلى GitHub: Settings → Secrets → Actions
5. حدّث SYNC_GITLAB_TOKEN بالـ Token الجديد
```

### 2. 📝 GitLab Project ID تغيّر

**المشكلة:**
- إذا أعيد إنشاء المشروع أو نُقل
- الـ Project ID يتغير

**الحل:**
```bash
1. افتح مشروعك في GitLab
2. اذهب إلى Settings → General
3. انسخ Project ID (رقم في أعلى الصفحة)
4. اذهب إلى GitHub: Settings → Secrets → Actions
5. حدّث SYNC_GITLAB_ID بالرقم الجديد
```

### 3. 🔒 صلاحيات GitLab تغيرت

**المشكلة:**
- صلاحياتك في المشروع تغيرت
- أصبحت أقل من Maintainer

**الحل:**
```bash
1. افتح المشروع في GitLab
2. اذهب إلى Project information → Members
3. تحقق من دورك (Role)
4. يجب أن يكون Maintainer أو Owner
5. إذا لم يكن كذلك، اطلب من المالك ترقية صلاحياتك
```

### 4. 🌐 مشكلة مؤقتة في الشبكة أو GitLab API

**المشكلة:**
- GitLab API قد يكون معطل مؤقتاً
- مشاكل في الشبكة

**الحل:**
```bash
1. تحقق من حالة GitLab: https://status.gitlab.com/
2. انتظر بضع دقائق
3. حاول مرة أخرى
```

## 🧪 الاختبارات

### اختبار 1: تشخيص شامل
```bash
python scripts/diagnose_gitlab_sync.py
```

### اختبار 2: محاكاة GitHub Actions
```bash
bash scripts/test_github_actions_simulation.sh
```

### اختبار 3: فحص حالة المزامنة
```bash
python scripts/test_gitlab_sync.py
```

## 🔍 فحص Workflow Logs في GitHub

### الطريقة:
```bash
1. اذهب إلى GitHub Repository
2. اضغط على Actions (في الأعلى)
3. اختر "Universal Repository Synchronization Protocol"
4. اضغط على آخر تشغيل (run)
5. اقرأ الـ logs بحثاً عن أخطاء
```

### رسائل الخطأ الشائعة:

#### ❌ "Authentication failed"
```
السبب: Token خاطئ أو منتهي الصلاحية
الحل: حدّث SYNC_GITLAB_TOKEN
```

#### ❌ "Project not found"
```
السبب: Project ID خاطئ أو المشروع محذوف
الحل: تحقق من Project ID وحدّث SYNC_GITLAB_ID
```

#### ❌ "Permission denied"
```
السبب: صلاحيات Token غير كافية
الحل: أنشئ Token جديد بصلاحيات api + write_repository
```

#### ❌ "403 Forbidden"
```
السبب: ليس لديك صلاحيات كافية في المشروع
الحل: اطلب دور Maintainer أو Owner
```

## ✅ التحقق من نجاح الإصلاح

### بعد تحديث الأسرار:

1. **اختبار يدوي:**
```bash
# في GitHub → Actions
1. اختر "Universal Repository Synchronization Protocol"
2. اضغط "Run workflow"
3. انتظر حتى ينتهي (~3 دقائق)
4. يجب أن ترى علامة خضراء ✓
```

2. **اختبار تلقائي:**
```bash
# اعمل test commit
git commit --allow-empty -m "test: verify GitLab sync"
git push

# راقب GitHub Actions
# يجب أن تظهر علامة خضراء ✓
```

3. **التحقق في GitLab:**
```bash
# افتح مشروعك في GitLab
# يجب أن ترى الـ commit الجديد خلال 5 دقائق
```

## 📊 قائمة فحص كاملة

```
□ GitLab Token لم ينتهِ
□ GitLab Token له صلاحيات api + write_repository
□ GitLab Project ID صحيح
□ أنت Maintainer أو Owner في المشروع
□ المشروع موجود ولم يُحذف
□ الـ Secrets موجودة في GitHub:
  □ SYNC_GITHUB_TOKEN
  □ SYNC_GITHUB_ID
  □ SYNC_GITLAB_TOKEN
  □ SYNC_GITLAB_ID
□ Workflow يعمل بدون أخطاء
□ Git configuration صحيحة
```

## 🚀 الإصلاح السريع (Quick Fix)

إذا كنت متأكد أن المشكلة في الـ Token:

```bash
# 1. أنشئ Token جديد في GitLab
https://gitlab.com/-/profile/personal_access_tokens

# 2. حدّث الـ Secret في GitHub
GitHub → Settings → Secrets → Actions → SYNC_GITLAB_TOKEN

# 3. اختبر
GitHub → Actions → Universal Sync → Run workflow

# 4. تحقق
GitLab → Repository → Commits (يجب أن تطابق GitHub)
```

## 💡 نصائح للوقاية

### 1. راقب تواريخ انتهاء الـ Tokens
- أنشئ Tokens بمدة صلاحية طويلة (سنة)
- ضع تذكير قبل انتهاء الصلاحية بشهر

### 2. راقب Workflow Runs
- تحقق من GitHub Actions بانتظام
- إذا ظهرت علامة حمراء ✗، افحص فوراً

### 3. احتفظ بنسخة احتياطية من الأسرار
- احفظ الـ Project ID في مكان آمن
- وثّق أي تغييرات في الإعدادات

## 📞 الحصول على المساعدة

إذا جربت كل شيء ولم تحل المشكلة:

1. شغّل أداة التشخيص:
```bash
python scripts/diagnose_gitlab_sync.py > diagnostic_report.txt
```

2. شاهد Workflow logs:
```bash
GitHub → Actions → Universal Sync → Latest run → Logs
```

3. تأكد من:
- ✅ الـ Token ساري المفعول
- ✅ الـ Project موجود
- ✅ لديك الصلاحيات المطلوبة

---

**ملاحظة:** في 99% من الحالات، المشكلة هي Token منتهي الصلاحية. ابدأ من هنا! 🔑
