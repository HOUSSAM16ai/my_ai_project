# 🌟 CogniForge: ترسانة الوكلاء الخارقين (The Super-Agent Arsenal)

> **"حيث يلتقي الذكاء الاصطناعي الفائق بالتخطيط المستقبلي لتغيير مسار البشرية."**

أهلاً بك في **CogniForge**. هذا ليس مجرد مشروع، بل هو **عقل رقمي متكامل** (Hive Mind) مصمم لإدارة وتنسيق جيش من الوكلاء الأذكياء (Agents) الذين يعملون باستقلالية تامة لتحقيق أهداف معقدة.

تم تبسيط هذا النظام المعقد بشكل "خرافي" ليصبح مفهوماً وسهل الاستخدام للمطورين الطموحين، مع الحفاظ على قوته الجبارة تحت الغطاء.

---

## 🧠 المفهوم الجوهري (The Core Concept)

تخيل النظام ككائن حي ذكي:

1.  **Overmind (العقل المدبر):** هو القائد الأعلى. يراقب، يخطط، ويوزع المهام على الوكلاء. لا يكتب الكود بيده، بل يوجه الأيادي التي تكتب.
2.  **Reality Kernel (الجسد/النواة):** هو تطبيق FastAPI الموحد الذي يربط كل الأعضاء ببعضهم. هو الواقع الذي يعيش فيه الوكلاء.
3.  **Engines (الأعضاء الحيوية):**
    *   **Space Engine:** يدير الذاكرة الدائمة (Database).
    *   **Time Engine:** يدير سياق الوقت والمهام (Context).
    *   **Energy Engine:** يمد الوكلاء بالقدرة الحسابية (AI Gateways).

---

## 🚀 كيف تبدأ؟ (Zero to Hero)

لقد قمنا بإزالة كل التعقيدات. تحتاج فقط إلى خطوة واحدة لتدخل هذا العالم.

### المتطلبات المسبقة
- Python 3.12+
- Docker (اختياري ولكنه مفضل)

### التشغيل الفوري

```bash
# 1. تثبيت الترسانة (Dependencies)
pip install -r requirements.txt

# 2. إيقاظ الكيان (Run Server)
./scripts/setup_dev.sh
```

الآن، النظام حي ويعمل على: `http://localhost:8000`

---

## 🗺️ خريطة النظام (System Map)

لقد قمنا بتنظيف كل "الأشياء الميتة". ما تراه هو ما يعمل فقط:

*   **`app/services/overmind/`**: مركز القيادة. هنا تجد المنطق الخاص بالتخطيط والتنفيذ الذاتي.
*   **`app/services/agent_tools/`**: صندوق الأدوات. الأدوات التي يستخدمها الوكلاء (قراءة ملفات، بحث، تشغيل أوامر).
*   **`app/kernel.py`**: نقطة البداية لكل الاتصالات (FastAPI App).
*   **`cli.py`**: أداة التحكم اليدوية في النظام (Command Line Interface).

---

## 🛠️ أدوات التحكم (Command Center)

لأنك أنت "المشغل" لهذا الكيان، لديك أدوات خاصة:

**اختبار صحة النظام:**
```bash
python3 -m pytest
```

**إعطاء أمر مباشر للنظام (CLI):**
```bash
python -m cli db create-all  # بناء الذاكرة
```

---

## 🔮 فلسفة المشروع

هذا المشروع مبني على مبادئ:
*   **الاستقلالية التامة (Full Autonomy):** الوكلاء يصححون أخطاءهم بأنفسهم.
*   **التفكير الذاتي (Self-Reflection):** النظام يراجع قراراته قبل التنفيذ.
*   **البساطة الخارقة:** تعقيد هائل في الداخل، وواجهة بسيطة جداً في الخارج.

---

> *تم التوثيق والتبسيط بواسطة: Jules (AI Architect)*
